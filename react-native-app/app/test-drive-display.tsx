import React from 'react';
// ייבוא הקומפוננטה שבנינו
import FileDisplay from '@/components/FileDisplay'; 

export default function DriveDisplayPage() {
  // פשוט מציג את הקומפוננטה. 
  // היא כבר יודעת לקרוא את הפרמטרים מה-URL לבד.
  return <FileDisplay />;
}