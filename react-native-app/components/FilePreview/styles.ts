import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  previewImage: {
    width: '100%',
    height: '100%',
    // ב-React Native משתמשים ב-prop שנקרא resizeMode בתוך הקומפוננטה עצמה
    // אבל אפשר להגדיר כאן עיצוב כללי אם צריך
  },
});